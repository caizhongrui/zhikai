# 🎯 内联 AI 补全使用指南

**类似 GitHub Copilot / 通义灵码的体验**

---

## ✨ 主要改进

### 1. **内联建议显示**（灰色文本）
- ✅ 不再显示右下角通知
- ✅ 直接在编辑器中显示灰色建议
- ✅ 类似 GitHub Copilot 的体验

### 2. **自动触发**
- ✅ 输入 2 个字符后自动显示建议
- ✅ 无需手动触发（也支持手动）

### 3. **多个建议**
- ✅ 提供 1-3 个不同长度的建议
- ✅ 可以用快捷键切换

### 4. **更好的提取逻辑**
- ✅ 正确提取 markdown 代码块内容
- ✅ 过滤对话式回复

---

## 🚀 快速开始

### 1️⃣ 重启 IDE

```bash
# 杀掉旧进程
pkill -f "天和·智开"

# 启动 IDE
cd /Users/caizhongrui/Documents/workspace/boyo/plugin/ide/src/tianhe-zhikai-ide
./scripts/code.sh .
```

### 2️⃣ 打开 Java 文件测试

创建 `Calculator.java`:
```java
public class Calculator {
    public int add(int a, int b) {
        return a + b;
    }

    public int sub   // 输入到这里，等待 1-2 秒
}
```

### 3️⃣ 查看内联建议

**自动触发**（输入后等待）：
```java
public int sub█tract(int a, int b) {
    return a - b;
}
```

**手动触发**（按快捷键）：
- **Cmd+Shift+P** → 输入 "AI Smart Completion"
- 或设置快捷键（默认无）

---

## ⌨️ 快捷键操作

### 接受建议
- **Tab** - 接受当前建议
- **Enter** - 接受当前建议（某些情况）
- **→** (右箭头) - 逐字接受

### 切换建议
- **Alt+]** - 下一个建议
- **Alt+[** - 上一个建议

### 拒绝建议
- **Esc** - 隐藏建议
- 继续输入 - 自动隐藏

---

## 🎨 使用场景

### 场景 1：方法补全

**输入**：
```java
public class UserService {
    private List<User> users = new ArrayList<>();

    public void addUser(User user) {
        users.add(user);
    }

    public User get   // 光标在这里
}
```

**AI 建议**（自动显示灰色文本）：
```java
public User getUserById(int id) {
    return users.stream()
        .filter(u -> u.getId() == id)
        .findFirst()
        .orElse(null);
}
```

**操作**：
- 按 **Tab** 接受整个建议
- 或按 **Alt+]** 查看其他建议

---

### 场景 2：多行建议切换

**建议 1**（单行）：
```java
getUserById(int id) {
```

**建议 2**（2行）：
```java
getUserById(int id) {
    return users.stream()
```

**建议 3**（3行）：
```java
getUserById(int id) {
    return users.stream()
        .filter(u -> u.getId() == id)
```

按 **Alt+]** 或 **Alt+[** 切换。

---

### 场景 3：空行补全

**有上下文时，空行也能补全**：
```java
report.append("参数1\n");
report.append("参数2\n");
   // 光标在空行，按 Cmd+Shift+P → AI Smart Completion
```

**AI 建议**：
```java
report.append("参数列表: ").append(context.getParameterList()).append("\n");
```

---

## 🔧 配置选项

### 切换 AI 模型

打开 **Settings** (`Cmd+,`)，搜索 `zhikai.ai`：

| 设置 | 值 |
|------|------|
| **zhikai.ai.model** | 选择模型 |
| **zhikai.ai.apiKey** | API Key（可选） |
| **zhikai.ai.enableCache** | 启用缓存 |
| **zhikai.ai.timeout** | 超时时间 |

**可选模型**：
- `qwen-coder-turbo` - 快速，推荐
- `qwen3-coder-480b-a35b-instruct` - 最新大模型
- `qwen-max` - 高质量
- `qwen-plus` - 平衡

### 自定义快捷键

**File → Preferences → Keyboard Shortcuts**

搜索 `AI Smart Completion`，设置快捷键，例如：
- `Alt+K`
- `Cmd+I`
- `Ctrl+Space`

---

## 📊 工作原理

### 内联建议流程

```
用户输入代码
    ↓
触发条件检查（2+ 字符 或 显式触发）
    ↓
获取上下文（前 10 行）
    ↓
构建 Prompt → AI API
    ↓
提取 & 过滤响应
    ↓
生成 1-3 个建议
    ↓
显示为灰色内联文本
    ↓
用户按 Tab 接受 或 Esc 拒绝
```

### 触发条件

| 触发方式 | 条件 | 延迟 |
|---------|------|------|
| **自动** | 输入 ≥ 2 字符 | ~1-2秒 |
| **手动** | 命令/快捷键 | 立即 |
| **空行** | 有上下文时 | 手动 |

---

## 🐛 故障排查

### 问题 1：没有显示内联建议

**检查项**：
1. ✅ 是否输入了至少 2 个字符？
2. ✅ 打开 **Developer Tools** → Console
3. ✅ 查找 `[AI Inline Completions] Provider registered`

**解决**：
```bash
# 重启 IDE
pkill -f "天和·智开"
./scripts/code.sh .

# 检查 Console 日志
# Help → Toggle Developer Tools
```

---

### 问题 2：建议显示但无法接受

**原因**：VS Code 的内联建议快捷键可能冲突

**解决**：
1. **Keyboard Shortcuts** (`Cmd+K Cmd+S`)
2. 搜索 `acceptInlineSuggestion`
3. 确保绑定到 **Tab** 或 **Enter**

---

### 问题 3：建议不准确

**优化方法**：

1. **切换模型**：
   ```json
   {
     "zhikai.ai.model": "qwen3-coder-480b-a35b-instruct"
   }
   ```

2. **提供更多上下文**：
   - 在方法/类内触发（而不是文件开头）
   - 添加注释描述意图

3. **重试**：
   - 按 **Esc** 隐藏当前建议
   - 重新触发（Cmd+Shift+P → AI Smart Completion）

---

### 问题 4：API 调用失败

**检查 Network**：
1. **Developer Tools** → **Network**
2. 筛选 `dashscope`
3. 查看 Response

**常见错误**：
| 状态码 | 原因 | 解决 |
|--------|------|------|
| 401 | API Key 无效 | 检查 `zhikai.ai.apiKey` |
| 429 | 请求过多 | 等待或增加 timeout |
| 500 | 服务器错误 | 稍后重试 |

---

## 📈 性能优化

### 减少延迟

1. **启用缓存**：
   ```json
   {
     "zhikai.ai.enableCache": true
   }
   ```

2. **使用更快的模型**：
   ```json
   {
     "zhikai.ai.model": "qwen-coder-turbo"
   }
   ```

3. **调整 timeout**：
   ```json
   {
     "zhikai.ai.timeout": 10000  // 10秒
   }
   ```

---

## 🎯 最佳实践

### 1. **提供清晰的上下文**

**好的示例**：
```java
// Calculate user age based on birth date
public int calc   // AI 能推断出 calculateAge
```

**不好的示例**：
```java
public int calc   // 缺少上下文，AI 可能猜错
```

### 2. **逐步接受建议**

- 先接受方法签名（第一行）
- 查看方法体是否符合预期
- 如果不对，按 **Esc** 并手动编写

### 3. **结合其他功能**

- 使用 VS Code 的代码补全（Ctrl+Space）
- AI 建议作为辅助
- 关键逻辑仍需人工审查

---

## 🔍 调试模式

### 查看详细日志

**Console 输出**：
```
[AI Inline Completions] Provider registered
[AI Inline Completions] Providing 3 suggestions
[AI Service] Calling Qwen API, prompt length: 487
[AI Service] API call successful, response length: 156
```

### 监控 API 请求

**Network 标签** → 筛选 `dashscope`：
- **Request Payload** - 查看发送的 prompt
- **Response** - 查看 AI 返回内容
- **Timing** - 查看响应时间

---

## 🆚 对比其他工具

| 功能 | 天和·智开 | GitHub Copilot | 通义灵码 |
|------|----------|---------------|---------|
| 内联建议 | ✅ | ✅ | ✅ |
| 多个建议 | ✅ | ✅ | ✅ |
| 自动触发 | ✅ | ✅ | ✅ |
| 本地部署 | ✅ | ❌ | ❌ |
| 自定义模型 | ✅ | ❌ | ❌ |
| 免费 | ✅ | ❌ | ✅（有限） |

---

## 📞 需要帮助？

**如果遇到问题，请提供**：

1. **Console 日志**（截图）
   - Help → Toggle Developer Tools → Console

2. **Network 请求**（截图）
   - Developer Tools → Network → dashscope 请求

3. **代码示例**
   - 您输入的代码
   - 期望的建议
   - 实际的建议（如果有）

4. **配置信息**
   - Settings → 搜索 `zhikai.ai`
   - 截图您的配置

---

## 🎉 享受 AI 辅助编码！

**提示**：
- 多尝试不同的触发方式
- 善用快捷键切换建议
- 结合人工判断，不要盲目接受

**更新日期**: 2025-11-04 21:03
**版本**: v2.0 - 内联建议版
